# orch-box tmux defaults — make multi-pane navigation easy for the human.
set -g mouse on             # click a pane to focus it; scroll wheel to scroll back
set -g history-limit 50000  # generous scrollback for watching agent output
set -g pane-border-status top
# (pane-border-format / @orch_role labels are set by `orch up`.)
